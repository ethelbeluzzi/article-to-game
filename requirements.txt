streamlit           # Framework para a interface web interativa
PyMuPDF             # Extração de texto de arquivos PDF
python-docx         # Leitura de documentos DOCX (Word)
requests            # Requisições HTTP (usado para acessar APIs como Hugging Face)
PyGithub            # Biblioteca para interagir com repositórios GitHub via API
html2text           # Conversão de HTML para texto plano
markdown2           # Conversão de markdown para HTML (ex: exibir explicações)
uuid                # Geração de identificadores únicos para jogos
numpy               # Utilitários matemáticos simples (opcional, para fases ou pontuação)

